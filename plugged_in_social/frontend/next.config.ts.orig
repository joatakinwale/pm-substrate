import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Skip type checking + linting in Docker builds (CI handles this separately)
  typescript: {
    ignoreBuildErrors: true,
  },
  eslint: {
    ignoreDuringBuilds: true,
  },
};

export default nextConfig;
