"""Liquid agent benchmark harness.

Deterministic, reproducible benchmarks proving (or refuting) that
Liquid's agent-UX features reduce token usage / improve recovery / etc.

Entry point: ``python -m benchmarks.run``.
"""

__all__: list[str] = []
