{
  "name": "@pm/integration-kit",
  "version": "0.1.0",
  "private": true,
  "description": "Generic zero-rewrite integration layer (ROADMAP D5): external adapter registry — outside tools (agent harnesses, browser QA harnesses, mapping proposers) register as tenant-scoped, substrate-governed adapters with pinned source, declared gates, and evidence fields. Registration is an admitted event; the registry is a fold over the log. No app-specific code lives here.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "clean": "rm -rf dist .tsbuildinfo",
    "test": "echo \"(tests run from root)\" && true"
  },
  "dependencies": {
    "@pm/agent-state-core": "workspace:*",
    "@pm/entity-mapping": "workspace:*",
    "@pm/events": "workspace:*",
    "@pm/graph": "workspace:*",
    "@pm/types": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "pg": "^8.13.0",
    "typescript": "^5.9.0",
    "vitest": "^2.1.0"
  }
}
