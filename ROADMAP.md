# pm-substrate — Roadmap

*The single forward-looking plan. Superseded plans: [`refactor-plan.md`](./refactor-plan.md) (executed 2026-07-02), [`docs/history/roadmap-2026-06.md`](./docs/history/roadmap-2026-06.md). Standing decisions live in the continuity ledger — `pnpm dev:resume` is authoritative over this file; if they disagree, record a superseding `decision` checkpoint and fix this file in the same change.*

---

## North star (decided 2026-07-02; proof order superseded 2026-07-13)

pm-substrate exists to make agent-run work more reliable by turning project-management state into an enforceable operational boundary. The product claim is causal and falsifiable: **on tasks that publicly expose agent-state failure, agents using pm-substrate must complete more of the benchmark's own tasks reliably than matched native and sham-sidecar controls, without hiding failures behind blocks, extra retries, or collateral state changes.** Internal events, receipts, and blocks diagnose the mechanism; they are never the outcome.

The eventual transfer targets remain the marketing lab (PluggedInSocial) and the hedge-fund lab (ArrowHedge), but they are frozen during the public-proof program. Neither app is used to design, tune, or score D6/D7. A public benchmark keep decision is required before app-boundary or business-operability work resumes. The executable proof protocol, failure modes, and thresholds are in [`docs/objective-falsification.md`](./docs/objective-falsification.md).

**Generic-first (decided 2026-07-06, chk_565b7eb9):** the substrate is NOT built for those two apps alone. Benchmark names and formats remain in peripheral adapters. No benchmark- or lab-specific code may enter `@pm/agent-state-core`; a missing general primitive may be added only after a public failure demonstrates the gap, and only with a non-test runtime consumer, ablation, and clean regression evidence.

**Falsification gate:** evidence maturity, not a calendar date, controls the decision. Qualification runs may debug the harness and set the confirmatory sample size, but cannot establish efficacy. The claim survives only after a held-out public benchmark shows task-level paired lift over both native and sham controls, anti-degenerate mutants fail, restart/dynamic-state guardrails hold, artifacts reproduce from a clean checkout, and the result replicates on a second benchmark or model. Otherwise repair, narrow, or kill the claim. If agents or evaluators route around the substrate, that is a failure.

## Hard requirements (non-negotiable product properties)

1. **Zero-rewrite integration.** A system adopts the substrate through a thin kit only: `mapping.yaml` (entity-mapping), a sync adapter (webhook/poll/CDC), an action executor (admitted action → the app's existing API). The substrate governs the **agent boundary**, never the app's internals. Proven pattern: ArrowHedge integrates as a vanilla upstream clone + read-only `/integration/v1/*` endpoints. CI-enforced on the substrate side by `validate:zero-edit`.
2. **Sidecar deployment.** Docker + Postgres in the user's environment (VPC or laptop). No hosted dependency, no data leaving.
3. **Agents integrate via protocol, not imports.** MCP tools (`resume`, `observe`, `propose`, `admit`, `checkpoint`) + HTTP. Kernel stays TypeScript (the type system is load-bearing); clients are language-agnostic; a thin Python client SDK ships when the labs need it.
4. **Control plane answers five questions** from the admitted log alone: What is being done? What did governance do (admitted / blocked / gated, by whom)? What did it cost (tokens, by session/agent)? What are the results (the eval metrics)? What got optimized (closed work + superseding decisions)? v0 is `pnpm dev:status`; v1 is the dashboard page.
5. **Adoption ramp is shadow-first.** Observe + warn with zero behavior change, produce the "what would have been blocked" report, then gate one action type at a time.
6. **No unconsumed primitives.** Every new export ships with a runtime consumer in the same change (the v62–v229 lesson; enforced by budgets/ratchet gates).
7. **Technical proof is not business proof.** Activity counts, fixture tests, and seeded failure blocks cannot by themselves satisfy “worth operating.” `pnpm pm:memo` applies the executable business-operability scorecard and a verdict ceiling from admitted evidence.
8. **Independent outcome oracle.** The benchmark-owned task oracle is primary and cannot import the substrate gate predicate. A block is success only when the unchanged upstream oracle says the task succeeded.
9. **Exact attempt identity.** Every result binds upstream URL and revision, license, task/split and corpus hashes, model/prompt/tool configuration, the provider sampling seed when supported (otherwise an explicit no-seed declaration plus stochastic repeat identity), substrate revision, arm, and unique attempt ID. Repeated attempts on one task are clustered, not counted as independent tasks.
10. **Placebo and anti-degenerate controls.** Compare native, equal-overhead sham sidecar, and real substrate arms. `block-all` and `allow-all` mutants, expected-allow cases, and irrelevant mutations must fail the qualification gate when appropriate.
11. **Authenticated, preregistered verification with separate decision authority.** Hashes, timestamps, signatures, and `independent=true` assertions are insufficient when the experiment caller selects their own trust policy. Decision tasks are the exact deterministic top-ranked subset of a predeclared eligible universe. Every D7 check references exactly one non-empty, kind-specific canonical observation; the gate binds its exact kind/check/subject and versioned procedure, validates strict fact keys, and rejects a signer-supplied result shortcut. The evidence is signed by an Ed25519 verifier whose owner differs from the experiment producer; the manifest is preregistered before execution; attempt times/raw roots are separately attested; and verifier keys/source revisions come from a policy pinned outside the result bundle. Crucially, adapter-specific procedures must derive the structured facts from the bound manifest, attempts, analysis, and content-addressed raw benchmark records. Until then, internally consistent signed facts are diagnostic only and the report must be `not_eligible`. Only after that derivation boundary exists may automated verification report conditional evidence eligibility. An authenticated owner must still separately authorize the exact report hash before any KEEP or app-unfreeze consequence.

## Phases (dogfood order)

| Phase | Deliverable | Done when |
|---|---|---|
| **D1 ✅ 07-02** | Dev-session loop (`dev:resume/checkpoint/handoff/cost/status`), CLAUDE.md protocol, seeded ledger | Sessions resume from the ledger, chain-verified; control-plane v0 answers the five questions in text |
| **D2 ✅ 07-06** | **MCP tool surface** (`packages/substrate-mcp`): `substrate_resume/observe/propose/admit/checkpoint`, stdio and streamable-HTTP transports, content-addressed ledger head, warn-first propose + enforced admit, verified stale-basis block | Done: config-line stdio mount plus streamable HTTP that refuses to start without bearer auth; gate and transport tests green |
| **D3 ✅ 07-03** | **Unblocked-work projection + dispatcher** (`computeUnblockedWork` + `dispatchUnblockedWork` in capability-pmgovernance-stage-gate; `pnpm pm:dispatch`): todo items with terminal-complete deps + passed gates dispatch to their RACI "A" role as `pm.work.dispatched` events, basis-hash deduped | Done, exercised end-to-end on the real graph (dedupe + dependency-completion reopening proven). Follow-up done 07-03: two-arm lab scenario `pm-governance-dispatch` (dependency regression after observe ⇒ baseline starts blocked work, dispatcher refuses at head) |
| **D4 ✅ 07-06** | **Control-plane dashboard**: substrate-http `/tenants/:id/control-plane` route (five questions from the admitted log), dashboard proxy + pure renderer, full integration test (blocked actions + dispatches + costs + chain integrity asserted over seeded state; surface opt-in via `controlPlanePool`) | Done end-to-end: route tested, proxy wired, renderer unit-tested |
| **D5 ✅ 07-06 (reframed 07-06)** | **Generic integration layer** (`packages/integration-kit`): (a) **external adapter registry** — outside tools register as substrate-governed adapters with pinned source (url+commit), declared capabilities, required gates, evidence fields; pi harness (earendil-works/pi agent runtime), canary (LopeWale/canary browser QA), and Liquid (ertad-family/liquid universal adapter runtime, vendored at `liquid/`, lanes L1–L5 in [`docs/liquid-integration-plan.md`](./docs/liquid-integration-plan.md)) are the registered adapters; (b) **declarative entity-mapping + idempotent sync-runner** (app's existing read endpoints → Tier-1 primitives, provenance-stamped); (c) **executor bridge** (admitted envelope → app's existing API) | Done against a neutral fixture app: register→list round-trip; sync twice = idempotent; mapped nodes carry source provenance; executor fires only on accepted envelopes |
| **D5-L ✅ 07-06 (Liquid lanes)** | Liquid periphery under governance ([`docs/liquid-integration-plan.md`](./docs/liquid-integration-plan.md)): **L1** runbook + local mount (owner env) · **L2** liquid-as-sync-source driver over the real `liquid-mcp` vocabulary (`liquid_connect`/`liquid_fetch`), `target_model` derived FROM the approved EntityMapping · **L3** `pm.mapping.proposed` + drift-as-obstruction + `pm:mappings` approvals · **L4** liquid executor target (`liquid_execute`, writes on accepted envelopes only) · **L5** live rehearsal (owner-gated) | Done: L2–L4 fixture-proven against the real vocabulary; unapproved hashes and missing external IDs obstruct; blocked envelopes never execute; L1/L5 recorded from the owner's environment against ArrowHedge |
| **D5-D ✅ 07-08 (Dashboard integration workbench)** | Unify `packages/substrate-dashboard` into a navigable substrate dashboard and add the D5 human adoption surface ([task index](./TASKS.md), [implementation plan](./docs/superpowers/plans/2026-07-08-dashboard-integration-workbench.md)): Control Plane, Local Agent Lab, and **Integration Workbench** (the plan's Live Metrics view shipped, then was removed the same day by owner decision "No lab-app content in the substrate dashboard" — lab-app metrics belong to the external apps). The workbench supports both config-first mapping (`mapping.yaml`/JSON validate → propose → approve → dry-run sync) and Liquid-assisted no-config proposal (`liquid_discover`/field inspection → pending `pm.mapping.proposed`), with all approvals, rejections, sync previews, and write outcomes read from the admitted log. | Done when the dashboard exposes mapping state, validation issues, pending/approved hashes, approve/reject actions, dry-run sync results, and Liquid-assisted starter proposals; unapproved mappings cannot sync; Liquid never writes directly; dashboard tests prove all routes return JSON and the visible shell mounts control-plane/lab/integration views |
| **D6-A ✅ implementation 07-13 (evidence reset)** | Reclassify local labs and synthetic evals as conformance diagnostics; harden identity and oracle independence | Done: no local scenario can pass by blocking every action; baseline/substrate attempts have unique exact identities; memos say external efficacy is unproven; block-all and allow-all mutants are red |
| **D6-B ◐ mechanism qualification complete; efficacy evidence ineligible (first public slice)** | Pinned ToolSandbox multi-turn cellular/message scenario through the real agent-state boundary | Fresh native/sham/substrate runs now replay the clean pinned Apple oracle, bind the exact clean starting context except 11 declared timestamps, cross-check raw provider frames/trajectory/state deltas, exercise authenticated HTTP sidecars, attest runtime closure, and prove real SIGKILL/reap/fresh-process restart. The derivative retained the adverse result: native and sham duplicated the target side effect while substrate blocked it, yet Apple's strict score remained 1.0 in every arm. This proves a bounded idempotency mechanism and an oracle blind spot, not task-level efficacy. Eligibility remains closed for provider usage/cost/latency, an independently trusted oracle/runtime and trust anchor, and a non-scripted public agent run |
| **D6-C ◐ phase-safe plan/command/raw contracts complete; official execution blocked (headline held-out proof)** | STATE-Bench Agent Learning Track qualification, confirmation, and distinct-model replication | The 20/80 train-only qualification/extraction partition, complete 150-task test phases, exact randomized per-cell commands, oracle-field-redacting adapter seam, one-attempt policy, externally pinned preregistration preflight, and command-bound raw schema are implemented. No official trajectory has run. Conversion remains closed until an instrumented executor retains independently anchored runner/provider/simulator/judge transports, hidden retries and terminal failures, actual model/economics, treatment uptake, replayable state, and raw-derived official scores |
| **D6-D ◐ two Sentinel designs falsified pre-outcome; production replacement hardening in progress (independent corner battery)** | Stress-test state loss, controls, restart/integrity, and changing-world behavior on pinned public scenarios | The old 27-cell MicroHub design is excluded: its useful memory was adapter-owned, speed 4 made positive trajectories unreachable, the no-op oracle did not prove monitoring, and Microsoft's exact paper agent was not released. The replacement runs at published speed 1 with one task-agnostic Chromium/Claude agent and four shape-matched arms: no-durable-state, sham continuity, plain durable KV, and production `PostgresContinuityLedger`. MicroHub is qualification-only. The frozen 12-task cross-app set is procedural holdout validation. The untouched 50-task catalog is content-frozen, but the proposed 19-relative-task × 3-repeat, true-lift=claim-threshold=0.10 power declaration is rejected: its exact necessary point-lift gate is at most 0.511210781855188 on the declared planning grid, before Holm/bootstrap/guardrails. Powered execution is blocked pending an externally replayable redesign. Completed replacement cells: 0 |
| **D6-E ◐ first public failure-derived repair traced; broader lift pending (failure-driven repair)** | Arrowsmith research and smallest runtime-consumed repair for gaps observed in D6-B–D | v232 traces the observed ToolSandbox HTTP-400 failure to an overlong raw idempotency key, applies the smallest domain-separated bounded digest repair in the real runner, and retests the exact case. The final derivative also exposes the upstream oracle's duplicate-side-effect blind spot. This is a bounded mechanism repair/qualification result; no held-out public lift means the general causal claim remains unproven |
| **D7 ◐ signed-assertion verifier complete; raw-derived semantic evidence, trust anchor, replication, and owner decision pending** | Clean-room artifact verification, second-benchmark/model replication, and keep/repair/kill memo | Signed preregistration and attempt-time/raw-root bindings verify; deterministic top-ranked task selection and paired confirmation beat both native and sham at the predeclared effect size; reliability, zero false-block/collateral, absolute/relative economics, and distinct-model replication pass. The current v4 report validates six receipts/31 structured assertions but marks them diagnostic-only and always `not_eligible`, because their facts are not adapter-derived from bound raw records. A future raw-derived path may report conditional eligibility; a separately authenticated owner must still authorize the report hash and narrow consequence |
| **D8 (deferred transfer)** | Re-open PluggedInSocial and ArrowHedge only if D7 keeps the public claim | Both apps attach with zero substrate edits and are evaluated under the separate business-operability scorecard; no public result is silently promoted into business value |

### Up next — public-proof execution sequence

The implementation/execution distinction and exact provider blockers are
recorded in [`docs/public-benchmark-status-2026-07-13.md`](./docs/public-benchmark-status-2026-07-13.md).
The old Sentinel implementation is now evidence of a successful falsification
gate, not evidence of benefit. No matched replacement matrix, external trust
anchor, replication bundle, or owner authorization exists. Prior ToolSandbox
and STATE-Bench artifacts remain diagnostic history; they do not displace the
current owner-directed Sentinel stress test.

1. **Falsify the instrument before spending outcomes.** Keep the rejected adapter-owned MicroHub matrix excluded. Close strict raw-verifier, process-identity, liveness, runtime-closure, and clean-checkout false-positive paths.
2. **Prove treatment isolation.** Use one task-agnostic browser agent and one fixed-width state API across `native`, `sham`, `plain-kv`, and `substrate`. Require restart, tenant/agent/scope isolation, concurrent append, tamper-fail-closed, and raw request/response replay tests before live calls.
3. **Run qualification, then procedural holdout.** At published speed 1, run the MicroHub relative/absolute/no-op triplet only to qualify the mechanism. Freeze and run the 12-task cross-application procedural holdout without replacement or outcome inspection. Neither phase may emit a material-benefit claim.
4. **Redesign, independently verify, then execute the powered confirmation.** The pre-outcome content universe is all 50 scenarios in the five untouched environments MicroChat, MicroDin, MicroFy, MicroGram, and MicroLendar: 19 relative/state-dependent tasks, 21 absolute controls, and 10 no-op controls; sorted task/hash manifest `48e1695b…`. The exact audit in `sentinel-production-power-audit.ts` blocks the current 19×3/10-point declaration. Separate the 10-point materiality threshold from the planning alternative and/or add predeclared information, then retain and externally verify the calculation bytes before signing. Only then anchor source, model, runtime, task, schedule, economics, and attempt-time raw roots outside the producer bundle and execute every declared cell without replacement.
5. **Arrowsmith only for an observed behavioral gap.** Harness defects are repaired as harness defects. Only a raw-verified substrate behavioral failure or a substrate-vs-plain-KV deficit can trigger research for the smallest general runtime-consumed repair, ablation, and the exact failed-case/control rerun.
6. **Replicate and decide.** Clean-room replay the complete raw universe, replicate on a separately frozen benchmark/model, and report task-level paired intervals, reliable success, false-contact/collateral rates, and cost/latency per strict success. Automation may report conditional eligibility; only a separately authenticated owner may keep, narrow, repair, or kill the claim.

**External-review response (decision checkpoints 2026-07-15/16; edits second per anti-drift rule).** The [2026-07-15 external review](./docs/external-review-2026-07-15.md) and its [code-verified response](./docs/external-review-response-2026-07-15.md) are preserved in docs. Standing rule adopted: **no new verification code until the next outcome cell completes** — enforced by the `evalPackageTotals` aggregate ratchet (`scripts/guardrail-ratchet.json`; raising a ceiling requires naming, in the same change, which completed run's failure or external skeptic demanded the code). Step 3's operational path is [`docs/phase-3-execution-runbook.md`](./docs/phase-3-execution-runbook.md) (entry point `pm-sentinel-production-batch`; the excluded smoke stays `pm-sentinel-production-smoke`). Two PROPOSED preregistration artifacts must be externally witnessed via `pnpm witness:anchor` **before the first behavioral cell**: the [outcome-interpretation matrix](./docs/outcome-interpretation-matrix-2026-07-16.md) (P1–P7, least-favorable-row rule) and the [state-effect endpoint amendment](./docs/state-effect-endpoint-amendment-2026-07-15.md) (strict score as non-inferiority guard, state-effect superiority claim). Step 4's ICC evidence comes from the step-3 pilot at its upper confidence limit. An open `decision-needed:` checkpoint holds the re-aim question (powered confirmatory at STATE-Bench vs Sentinel); the recorded recommendation is to decide after the pilot ICC exists.

## Later ladder (only after D7 keeps)

D8 business-transfer validation · commitments and conflicts as first-class objects (cross-department interfaces) · role-bound agent identities (org chart as write permissions) · PM-as-projection reporting pack · `pm-substrate-client` on PyPI · shrinking the excised/quarantined legacy surface.

## Anti-drift rules for this file

Roadmap changes are `decision` checkpoints first, edits here second. Until a separately authenticated D7 owner decision exists, do not modify or use PluggedInSocial or ArrowHedge as efficacy evidence. Benchmark names belong only in peripheral adapters. The official benchmark oracle must remain independent of substrate gating, and untouched headline results must remain separate from derivative fault-injection results. Research may propose; only observed-failure-driven, runtime-consumed code counts toward a causal repair. No event, block, receipt, retry, local-fixture pass, or self-selected trust policy may proxy for strict public task completion, reliability, collateral effects, cost, replication, or decision authority.
