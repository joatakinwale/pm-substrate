{
  "name": "@pm/evals",
  "version": "0.1.0",
  "private": true,
  "description": "Evaluation schema and validators for pm-substrate state-coherence experiments.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "clean": "rm -rf dist .tsbuildinfo",
    "test": "echo \"(tests run from root)\" && true"
  },
  "dependencies": {
    "@pm/agent-state": "workspace:*",
    "@pm/graph": "workspace:*",
    "@pm/registry": "workspace:*",
    "@pm/types": "workspace:*",
    "@pm/workflow": "workspace:*"
  },
  "devDependencies": {
    "@pm/capability-kit": "workspace:*",
    "typescript": "^5.9.0",
    "vitest": "^2.1.0"
  }
}
