{
  "name": "@pm/profile-registry",
  "version": "0.1.0",
  "private": true,
  "description": "Profile-definition registry + write-time validator. Substrate enforces the profile catalog declared by tenant-installed profile packages.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "clean": "rm -rf dist .tsbuildinfo",
    "test": "echo \"(tests run from root)\" && true"
  },
  "dependencies": {
    "@pm/types": "workspace:*",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@pm/profile-wedding": "workspace:*",
    "@types/pg": "^8.11.10",
    "typescript": "^5.9.0",
    "vitest": "^2.1.0"
  }
}
