{
  "name": "@pm/capability-finance-research-ingest",
  "version": "0.1.0",
  "private": true,
  "description": "finance-research.ingest capability skeleton for ArrowHedgeLabs Axis A event ingestion.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "clean": "rm -rf dist .tsbuildinfo",
    "test": "echo \"(tests run from root)\" && true"
  },
  "dependencies": {
    "@pm/profile-finance-research": "workspace:*",
    "@pm/registry": "workspace:*",
    "@pm/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^2.1.0"
  }
}
