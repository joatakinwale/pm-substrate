{
  "name": "@pm/substrate-http-demo",
  "version": "0.1.0",
  "private": true,
  "description": "Sample server entry point demonstrating @pm/substrate-http wired with the wedding profile + wedding-budget capability. NOT part of the substrate library; a runnable demo for docker-compose and local development.",
  "type": "module",
  "main": "./dist/server.js",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "clean": "rm -rf dist .tsbuildinfo",
    "start": "node dist/server.js",
    "dev": "tsx src/server.ts",
    "test": "echo \"(tests run from root)\" && true"
  },
  "dependencies": {
    "@pm/capability-wedding-budget": "workspace:*",
    "@pm/events": "workspace:*",
    "@pm/graph": "workspace:*",
    "@pm/profile-registry": "workspace:*",
    "@pm/profile-wedding": "workspace:*",
    "@pm/projections": "workspace:*",
    "@pm/registry": "workspace:*",
    "@pm/substrate-http": "workspace:*",
    "@pm/types": "workspace:*",
    "pg": "^8.13.0",
    "@pm/tenants": "workspace:*"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10",
    "tsx": "^4.21.0",
    "typescript": "^5.9.0"
  }
}
