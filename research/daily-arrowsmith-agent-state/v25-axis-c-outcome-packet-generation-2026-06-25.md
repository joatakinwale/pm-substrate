# Agent-State Arrowsmith v25: Axis C Outcome Packet Generation

Date: 2026-06-25
Status: research-to-runtime continuation
Immediate predecessor read: `research/daily-arrowsmith-agent-state/v24-live-outcome-envelope-store-2026-06-25.md`

## Loop State Update

Active question set entering this run: RQ12-RQ20, RQ26 from v24.

Answered this run:

| Eliminated question | Peer-reviewed answer | Repo action | Replacement question |
| --- | --- | --- | --- |
| RQ26: How should Axis C dynamic local-lab and workflow runtime runs generate and persist promoted outcome-envelope packets before EvalEvents are emitted, so local-lab pass claims are backed by live packets rather than synthetic fixture ids? | Runtime should emit the terminal packet at the same boundary that decides whether an action became operational, then persistence should write that packet before the measuring event. Paxos Made Live shows the engineering lesson that a replicated system needs explicit runtime records around the chosen operation, not just the abstract algorithm. ARIES supports recovery from durable log records written around transaction finality. Chandy/Lamport requires a recoverable recorded state instead of private participant views. Sagas require explicit step finality for long-running workflows. Therefore Axis C needs packets generated by the substrate arm itself, plus an eval runner that persists packets before EvalEvents cite them. | The deterministic Axis C eval scaffold now builds hash-valid canonical `ActionOutcomeEnvelope` packets for each substrate scenario and exposes them on `LocalLabSuiteResult`. `scripts/run-local-lab-evals.ts` records those packets through `PostgresEvalEventStore.recordActionOutcomeEnvelopes()` before recording EvalEvents. The dynamic `@pm/local-agent-lab` engine now builds canonical packets for admitted or refused arm runs and exposes them on `ArmRun`, `ScenarioRun`, and `SuiteResult`. | RQ27: How should dynamic `@pm/local-agent-lab` runs be converted into full `EvalEvent`s and persisted end-to-end against Postgres/Ollama so every Axis C failure class can prove packet-backed pass/fail decisions, not only the deterministic scaffold and stale-observation engine test? |

Active question set leaving this run: RQ12-RQ20, RQ27.

## Sources

- Tushar D. Chandra, Robert Griesemer, and Joshua Redstone, "Paxos Made Live: An Engineering Perspective," PODC, 2007: https://dl.acm.org/doi/10.1145/1281100.1281103
- C. Mohan et al., "ARIES: A Transaction Recovery Method Supporting Fine-Granularity Locking and Partial Rollbacks Using Write-Ahead Logging," ACM TODS, 1992: https://dl.acm.org/doi/10.1145/128765.128770
- K. Mani Chandy and Leslie Lamport, "Distributed Snapshots: Determining Global States of Distributed Systems," ACM TOCS, 1985: https://dl.acm.org/doi/10.1145/214451.214456
- Hector Garcia-Molina and Kenneth Salem, "Sagas," ACM SIGMOD, 1987: https://dl.acm.org/doi/10.1145/38713.38742

## Implementation Delta

1. `runLocalLabPairedScenario()` now returns `actionOutcomeEnvelopes` aligned with substrate `action_outcome_envelope` EvalEvent refs.
2. `runLocalLabPairedEvals()` now returns all generated Axis C scaffold packets.
3. `scripts/run-local-lab-evals.ts` persists packets before EvalEvents when `PM_DATABASE_URL` is available.
4. `@pm/local-agent-lab` now depends on `@pm/agent-state` and can build canonical action outcome packets for dynamic arm runs.
5. Dynamic `ArmRun`, `ScenarioRun`, and `SuiteResult` now expose generated `ActionOutcomeEnvelope` packets.
6. `docs/state-validation/local-agent-lab-scenarios.md` now documents the packet-before-EvalEvent replay contract.

## Falsification Criteria

This slice fails if:

1. A local-lab substrate EvalEvent cites an `action_outcome_envelope` id that has no generated packet.
2. Generated local-lab packets fail `verifyActionOutcomeEnvelopeHash()`.
3. The DB-backed local-lab runner records EvalEvents before packets.
4. The dynamic engine can return an admitted/refused arm run without a terminal packet.
5. The deterministic scaffold is mistaken for complete live Axis C coverage.

## Proof Status

| Axis | Current status |
| --- | --- |
| Axis A finance | Unchanged in this slice; Axis A still has replay-backed packet recovery and can use the live packet store. |
| Axis B marketing | Still blocked. PluggedInSocial is not restored/cloned and no accepted authoritative agency fixtures have been provided, so the whole solution remains unverified. |
| Axis C local lab | Improved. Deterministic Axis C EvalEvents now have generated canonical packets, the DB runner persists packets before events, and the dynamic engine exposes packets for real arm runs. Full dynamic end-to-end EvalEvent generation/persistence across all failure classes remains open. |

## Next Action Queue

1. Answer RQ27 by defining conversion from dynamic `ScenarioRun` records into EvalEvents with packet refs.
2. Run at least stale-observation through the dynamic local-lab engine with a live DB/Ollama-capable environment and persist packets plus EvalEvents.
3. Extend dynamic Axis C scenario specs beyond stale observation toward the ten failure classes.
4. Keep Axis B blocked until PluggedInSocial is restored or authoritative agency fixtures are explicitly accepted.
